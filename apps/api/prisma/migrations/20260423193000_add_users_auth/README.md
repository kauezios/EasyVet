## Migration Note

Este diretório registra a migration de base para autenticação e gestão de usuários.

Inclui:
- enum `AccessRole`
- tabela `User`
- índices de performance (`role`, `active`) e unicidade em `email`

